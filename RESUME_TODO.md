# Resume file

`public/resume.pdf` is generated from your uploaded Word resume and is
already wired up to every "Download Resume" button on the site (via
`resumeFile` in `src/data/resume.js`).

If you update your resume later, just replace `public/resume.pdf` with
the new PDF — no code changes needed.
