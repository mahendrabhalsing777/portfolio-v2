# One thing left to do

Drop your actual `resume.pdf` file into the `public/` folder
(as `public/resume.pdf`) so the "Download Resume" buttons work.
Vite serves everything in `public/` from the site root automatically.
